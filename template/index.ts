import type { Coords } from '../../types';

import { InputUtil, OutputUtil, MathUtil, ArrayUtil } from '../../utils';

const lines = InputUtil.strToLines(InputUtil.getInput());

let partOne = 0;
let partTwo = 0;

OutputUtil.outResult(partOne);
OutputUtil.outResult(partTwo);
